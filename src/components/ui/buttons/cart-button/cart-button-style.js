import { styled, space, layout, color } from "styled";

export const StyledCartButton = styled.button`
  display: flex;
  align-items: center;
  padding: 5px;
  cursor: pointer;
  ${space}
  ${layout}
  ${color}
`;
