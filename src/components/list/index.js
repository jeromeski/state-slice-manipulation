import React from "react";
import ProductCard from "../card";
import Feed from "./list-style";

const ProductFeed = () => {
  return (
    <Feed>
      <ProductCard />
    </Feed>
  );
};

export default ProductFeed;
